syntax = "proto3";

option csharp_namespace = "IoTClient.gRPC";
import "google/protobuf/timestamp.proto";

package message;

service EdgeGateway {
	//Unary RPC
  rpc Send (IoTMessage) returns (EdgeResponse);
  // Client streaming
  rpc SendStream (stream IoTMessage) returns (EdgeResponse);

}

message IoTMessage {
  string data = 1;
  google.protobuf.Timestamp _sendTime=2;
}

message EdgeResponse {
  google.protobuf.Timestamp _receivedTime=1;
}
